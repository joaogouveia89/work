var modal = new BS.Modal(document.getElementById('journalModal'));

$('#modal-journal-title').html("<%= j @journal.updated_at.strftime("%d/%m/%Y")  %>")

$('#modal-journal-body').html("<%= j render('show', journal: @journal) %>")

modal.toggle();
